#include "holberton.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * strtow - convert command line text args to strings
 * @str: passed string to be parsed
 *
 * Return: arguments as line separated strings
 */

char **strtow(char *str)
{
	int i, j;
	char *ptr;

	if (str == "" || str == NULL)
	{
		return (NULL);
	}
}
