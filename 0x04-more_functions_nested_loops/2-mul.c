#include "holberton.h"
/**
 * mul - prints the alphabet.
 *
 * @a: is passed from main function
 * @b: is passed from main function
 * Return: Always 0.
 */
int mul(int a,  int b)
{
	int r;

	r = (a * b);
	return (r);
}
