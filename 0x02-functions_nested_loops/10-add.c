#include "holberton.h"
/**
 * add - returns the last digit
 *@a: is an integer to be added
 *@b: is an integer to be added
 *
 * Return: is 0 if zero, 1 if positive, -1 if negative.
 */
int add(int a, int b)
{
	int c;

	c = (a + b);
	return (c);
}
